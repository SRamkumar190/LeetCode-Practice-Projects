class Solution {
    public int missingNumber(int[] nums) {
        int allNums = nums.length; 

            for (int i = 0; i < nums.length; i++)
            {
                allNums += i - nums[i]; 
            }
        
    return allNums; 
    }
}
